# Historical evaluation snapshots

These JSON reports were tracked under `eval_results/` before the single-root
migration. Their exact Git-normalized blobs are preserved here as historical
evidence. Runtime copies now live under the ignored
`workspace/runtime/evaluations/` directory. These snapshots are not evidence
of current model quality or canonical checkpoint selection.
